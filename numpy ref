np.add(a, b)        # same as a + b
np.subtract(a, b)   # same as a - b
np.multiply(a, b)   # same as a * b
np.divide(a, b)     # same as a / b
np.power(a, 2)      # same as a ** 2
np.mod(a, 2)        # same as a % 2
np.sum(a)        # total of all elements
np.mean(a)        # average
np.median(a)        # middle value
np.std(a)             # standard deviation
np.var(a)               # variance
np.min(a), np.max(a)   # smallest/largest
np.ptp(a)      # "peak to peak" = max - min
a = np.array([[1,2,3],[4,5,6]])
np.mean(a)            # 3.5  -- everything averaged together
np.mean(a, axis=0)      # [2.5, 3.5, 4.5] -- average DOWN each column
np.mean(a, axis=1)        # [2.0, 5.0] -- average ACROSS each row
np.round(3.567, 1)   # 3.6
np.floor(3.7)          # 3.0 -- always rounds down
np.ceil(3.2)              # 4.0 -- always rounds up
np.trunc(3.7)                # 3.0 -- chops off decimal, toward zero
np.sqrt(9)         # 3.0
np.cbrt(27)          # 3.0 -- cube root
np.exp(1)             # e^1 = 2.718...
np.log(np.e)          # natural log, = 1.0
np.log10(100)          # log base 10, = 2.0
np.log2(8)            # log base 2, = 3.0
np.sin(np.pi/2)     # 1.0
np.cos(0)              # 1.0
np.tan(0)                 # 0.0
np.degrees(np.pi)           # convert radians -> degrees: 180.0
np.radians(180)                # convert degrees -> radians: 3.14159...
np.abs(-5)         # 5
np.sign(-5)      # -1(returns -1, 0, or 1)
np.conj(z)              # complex conjugate
np.angle(z)               # phase in radians
z.real, z.imag              # real/imaginary parts (works on arrays too)
np.unwrap(phases)             # removes 2*pi jumps in a phase array — useful when plotting phase vs frequency
np.min(a), np.max(a)
np.argmin(a), np.argmax(a)    # INDEX of min/max, not the value
np.std(a), np.var(a)
np.cumsum(a)                     # running total
np.clip(a, 0, 1)                  # clamp values into [0,1]
np.round(a, 2)                      # round to 2 decimals
np.sqrt(a), np.log(a), np.log2(a)
np.dot(a, b)                          # matrix/vector product
a @ b                                   # same as np.dot for 2D arrays

arr = np.full(shape, 1+0j, dtype=complex)